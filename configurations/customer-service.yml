server:
  port: 8090
spring:
  jpa:
    hibernate:
      ddlAuto: update
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3307/customer?serverTimezone=UTC
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka